import type { ProjectConfig } from "@/types/content"

// Real completed projects only. Add an entry after the owner supplies permissioned
// photography, location, carpet area, invoiced budget band, scope and duration.
export const projects: ProjectConfig[] = []
